# MoonLabelKit：OSC 2026 项目说明

## 基本信息

- 项目：MoonLabelKit，标注数据一致性检查与质量治理工具包
- 参赛者：sgacc；GitHub：<https://github.com/Zy789kl/moonlabelkit>
- GitLink：<https://gitlink.org.cn/sgacc/moonlabelkit>
- 模块：`Zy789kl/moonlabelkit`
- 语言：MoonBit；目标：Native、Wasm、Wasm-GC
- 许可证：Apache License 2.0，见根目录 `LICENSE`

## 项目价值

AI 数据集进入训练前，常见风险包括重复样本、标注者冲突、Schema 越界、空文本、长度异常、训练测试泄漏和序列 span 越界。MoonLabelKit 将解析、统计、标注一致性、质量规则和报告输出组合成可嵌入 SDK 与可运行 CLI，能够作为数据集发布或训练流水线的质量门禁。

## 已实现范围

1. `core`：统一 Dataset、Sample、Annotation、分类/多标签/序列 span/评分标签模型，以及结构化治理错误。
2. `parser`：JSONL、CSV/TSV 解析，多标注记录按样本 ID 合并，行号错误反馈，序列 span 边界与文本一致性检查。
3. `stats`：标签分布、Shannon 熵、Gini、不平衡比、序列片段密度、按 split 覆盖统计和跨 split 泄漏率。
4. `agreement`：Cohen's Kappa、Fleiss' Kappa、Krippendorff's Alpha 和两两一致性矩阵。
5. `quality`：重复样本、标签冲突、Schema/长度异常、结构化质量规则和 0--100 可解释质量评分。
6. `report`/`cmd`：Markdown 与 HTML 报告，以及 `moon run cmd` 演示入口。

## 工程与验证

仓库包含 36 个手写 `.mbt` 文件，当前约 3000 行有效 MoonBit 源码（含测试，不含 `_build` 生成物），覆盖 27 个测试用例。已在 MoonBit 0.10.3 工具链上通过：

```text
moon fmt
moon check --deny-warn
moon test --target wasm-gc --deny-warn
moon build --target wasm-gc
```

GitHub Actions `.github/workflows/ci.yml` 在 Linux、macOS、Windows 上执行格式检查、`moon info`、`moon check --deny-warn`、全目标 debug/release 测试。真实场景样例位于 `examples/`，包括多标注者情感分类和 NER 序列标注。

## 来源与合规

核心实现为本项目原创 MoonBit 代码，仅依赖 MoonBit 官方标准库 JSON 与数学模块。示例数据由本项目编写，用于演示解析和治理，不包含个人敏感信息或商业数据。新增第三方代码、生成代码、夹具或数据时必须在 README/专门说明中记录来源、许可证与再分发权利。

## 交付与发布

默认分支为 `main`，提交遵循 Conventional Commits。发布前在干净工作树执行 CI 命令、`moon publish --dry-run`，确认模块名、版本、README、许可证和仓库链接一致，再执行 `moon publish`。Mooncakes 服务端发布状态和审核结果需要在发布后通过官方页面确认，不能由本地仓库单独保证。
